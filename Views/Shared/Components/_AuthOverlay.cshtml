@* Reusable component for auth overlay on homepage *@

<div class="auth-overlay">
    <h2>Join TruePal</h2>
    <p>Connect with travelers and share your favorite spots across the Philippines.</p>
    <div class="btn-group-auth">
        <a asp-controller="Auth" asp-action="Login" class="btn btn-primary">Log In</a>
        <a asp-controller="Auth" asp-action="Register" class="btn btn-outline-primary">Sign Up</a>
    </div>
</div>
